name: Hello World Workflow

on:
  push:  # si attiva quando fai push

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: echo "Hello, World from GitHub Actions!"
